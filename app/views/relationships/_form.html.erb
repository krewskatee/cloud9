<%= form_for @relationship, url: '/friends', method: :post, remote: true do |f| %>
  <%= f.hidden_field :befriender_id, value: current_user.id %>
  <%= f.hidden_field :status, value: "pending" %>
  <%= f.text_field :friend_id %>
  <%= f.submit "Add", class: 'btn btn-sm btn-info'%>
<% end %>
