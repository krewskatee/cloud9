<div class="add-chat">
  <%= form_for @chat, url: "/chats", method: :post do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.text_field :name %>
    <%= f.submit "Create Chat", class: "btn btn-large btn-primary"%>
  <% end %>
</div>

<% @chats.each do |chat| %>
<h3><%= chat.name %></h3>
<h1 class="chat_link"><%= link_to chat.name, "/chats/#{chat.id}", method: :get%></h1>
<% end %>
